{
  "title": "Versatile Thermostat Adaptive TPI",
  "config": {
    "step": {
      "global": {
        "title": "Adaptive TPI defaults",
        "data": {
          "minimal_activation_delay": "Minimal activation delay",
          "minimal_deactivation_delay": "Minimal deactivation delay",
          "responsiveness": "Responsiveness",
          "default_kint": "Initial Kint (indoor gain)",
          "default_kext": "Initial Kext (outdoor gain)",
          "adaptive_tpi_debug": "Debug mode"
        },
        "data_description": {
          "responsiveness": "Controls setpoint tracking speed. 1 = aggressive (fast rise, higher overshoot risk), 5 = conservative (slow rise, robust). Default: 3.",
          "default_kint": "Starting value for the proportional indoor gain. The algorithm will adapt it over time.",
          "default_kext": "Starting value for the outdoor feedforward gain. The algorithm will adapt it over time."
        }
      },
      "thermostat": {
        "title": "Adaptive TPI per-thermostat",
        "data": {
          "target_vtherm_unique_id": "Target thermostat",
          "minimal_activation_delay": "Minimal activation delay",
          "minimal_deactivation_delay": "Minimal deactivation delay",
          "responsiveness": "Responsiveness",
          "default_kint": "Initial Kint (indoor gain)",
          "default_kext": "Initial Kext (outdoor gain)",
          "adaptive_tpi_debug": "Debug mode"
        },
        "data_description": {
          "responsiveness": "Controls setpoint tracking speed. 1 = aggressive (fast rise, higher overshoot risk), 5 = conservative (slow rise, robust). Default: 3.",
          "default_kint": "Starting value for the proportional indoor gain. The algorithm will adapt it over time.",
          "default_kext": "Starting value for the outdoor feedforward gain. The algorithm will adapt it over time."
        }
      }
    },
    "abort": {
      "already_configured": "This Adaptive TPI entry is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Adaptive TPI defaults",
        "data": {
          "minimal_activation_delay": "Minimal activation delay",
          "minimal_deactivation_delay": "Minimal deactivation delay",
          "responsiveness": "Responsiveness",
          "default_kint": "Initial Kint (indoor gain)",
          "default_kext": "Initial Kext (outdoor gain)",
          "adaptive_tpi_debug": "Debug mode"
        },
        "data_description": {
          "responsiveness": "Controls setpoint tracking speed. 1 = aggressive (fast rise, higher overshoot risk), 5 = conservative (slow rise, robust). Default: 3.",
          "default_kint": "Starting value for the proportional indoor gain. The algorithm will adapt it over time.",
          "default_kext": "Starting value for the outdoor feedforward gain. The algorithm will adapt it over time."
        }
      }
    }
  }
}
